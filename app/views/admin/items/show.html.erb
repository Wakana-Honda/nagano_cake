<h2>商品詳細</h2>

 <table class="table">
  <tbody>
   <tr>
    <td>
     <!--books/show参照にしてね-->
    </td>
    <td><%= @item.name %></td>
    <td><%= @item.introduction %></td>
    <td><%= @item.genre.name %></td>
    <td><%= @item.price %></td>
    <td><%= @item.is_active %></td>
    <td><%= link_to "編集する", edit_admin_item_path,class:"btn btn-sm btn-success" %></td>
   </tr>
  </tbody>
  </table>