<div class="container">
 <div class="p-3 mb-2 bg-light text-dark">注文履歴一覧</div>

 <table class="table">
   <thead>
    <tr>
     <th>購入日時</th>
     <th>購入者</th>
     <th>注文個数</th>
     <th>注文ステータス</th>
    </tr>
   </thead>
   
   <tbody>
   <% @orders.each do |order| %>
    <tr>

    <td><%= link_to order.created_at.strftime('%Y/%m/%d'), admin_order_path(order) %></td>
    <td><%= order.name %></td>
    <td>
     <!--< @order_items.each do |oder_item| %>-->
      <!--<= order.order_details.amount %>-->
     <!--< end %>-->
    </td>
    <td></td>

    </tr>
   <% end %>
  </tbody>
  
  </table> 
  
  
</div>
